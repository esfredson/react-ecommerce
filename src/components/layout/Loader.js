import React from 'react'

const Loader = () => {
  return (
    // aplicamos la clase loader al nuevo componente Loader. Du efecto es como una demi-circunferencia girando.
    <div className="loader" />
  )
}

export default Loader